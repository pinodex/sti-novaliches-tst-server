{% extends '../../../layouts/dashboard' %}

{% set page_title = 'Stages' %}
{% set active_menu = 'categories' %}

{% block subcontent %}
  <section class="hero is-light has-contents-below">
    <div class="hero-body">
      <div class="container is-fluid">
        <div class="level">
          <div class="level-left">
            <div>
              <h1 class="title">Category Stages</h1>
              <h2 class="subtitle">{{ category.name }}</h2>
            </div>
          </div>

          <div class="level-right">
            <div class="field is-grouped">
              <p class="control">
                <a href="{{ route('dashboard.categories.stages.add', { category_id: category.id }) }}" class="button is-primary">
                  <span class="icon is-small">
                    <i class="fa fa-plus"></i>
                  </span>

                  <span>Add Stage</span>
                </a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="container is-fluid">
    <table class="table">
      <thead>
        <tr>
          <th></th>
          <th>Name</th>
          <th class="is-fit">Actions</th>
        </tr>
      </thead>

      <tbody>
        {% for stage in result.toJSON() %}
          <tr>
            <td class="is-fit">{{ stage.level }}</td>
            <td>{{ stage.name }}</td>
            <td>
              <div class="field is-grouped">
                <p class="control">
                  <a href="{{ route('dashboard.categories.stages.edit', { category_id: category.id, id: stage.id }) }}" class="button is-primary is-small is-inverted">
                    <span class="icon is-small">
                      <i class="fa fa-pencil"></i>
                    </span>
                  </a>
                </p>

                <p class="control">
                  <a href="{{ route('dashboard.categories.stages.delete', { category_id: category.id, id: stage.id }) }}" class="button is-danger is-small is-inverted">
                    <span class="icon is-small">
                      <i class="fa fa-times"></i>
                    </span>
                  </a>
                </p>
              </div>
            </td>
          </tr>
        {% else %}
          <tr>
            <td colspan="3">No results found</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
{% endblock %}
